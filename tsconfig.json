{
  "extends": "expo/tsconfig.base",

  // This is where 'include' belongs: at the root level.
  "include": ["**/*.ts", "**/*.tsx", "./src/types/reanimated.d.ts"],
  "exclude": ["node_modules", "__tests__", "**/*.test.ts", "**/*.test.tsx", "supabase/functions/**/*"  ],

  "compilerOptions": {
    "strict": true,
    "types": ["react-native-reanimated", "node", "jest"],
    "skipLibCheck": true
  },
}
